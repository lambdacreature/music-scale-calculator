export type ScaleDegree = {
  degree: string;
  note: string;
};

export const isValidNote = (candiate: string): boolean => {
  return true;
}; 

export const isValidInterval = (candiate: string): boolean => {
  return true;
}; 

export const isValidFormula = (candiate: string): boolean => {
  return true;
}; 
